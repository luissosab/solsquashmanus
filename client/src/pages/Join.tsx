/**
 * Sol mural language remains unchanged: this route places the existing adult and junior membership content in the prototype-aligned Join slot.
 */
import PlayAndPricing from "./PlayAndPricing";

export default function Join() {
  return <PlayAndPricing section="join" />;
}
