/**
 * Sol mural language remains unchanged: this route simply places the existing play content in the prototype-aligned Play navigation slot.
 */
import PlayAndPricing from "./PlayAndPricing";

export default function Play() {
  return <PlayAndPricing section="play" />;
}
