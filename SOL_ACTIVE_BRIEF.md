# Sol Squash — Active Working Brief

## Current source of truth

The latest stable checkpoint is `b42e7495`. It contains the approved website-first Sol architecture, the direct-but-late Playbypoint handoff model, truthful session framing, FAQ resolution paths, support contact placement, restrained cyan usage, removed diagonal background texture, and the current Home edits.

The protected comparison points remain `bff2498a` for the pre-website-first alternative and `3064699b` for the Founders’ Version. Do not reset or rewrite history; use reversible checkpoints for every focused change.

## Non-negotiable product model

The Sol website is the complete public source of truth for identity, storytelling, positioning, programme knowledge, pricing context, memberships, policies, support, and visitor confidence. Playbypoint is only the final operational transaction step after a visitor has selected an action. Do not add an iframe, calendar grid, faux checkout, fake availability, fake reviews, or unverified activity deep links.

## Non-negotiable visual model

Keep the mural-led Miami clubhouse system: supplied logo and real Sol imagery, looping Home video, navy/teal/green/orange/blue/cyan palette, mustard `#F4A911` as the yellow exception, rounded-3xl surfaces, cyan wave seams, off-centre two-dot squash balls, court-line motifs, and Lenis/Framer Motion interactions. Do not add generic SaaS patterns, diagonal stripe textures, arbitrary shapes, generated logos, or unrequested graphic systems. All supplied copy remains verbatim in the DOM; use limited progressive disclosure instead of deletion.

## Current Home state

The Home hero retains the address, `MIAMI'S SQUASH HOME.`, `BUILT BY PLAYERS. MADE FOR EVERYONE.`, and an internal `PICK YOUR WAY IN` action. The Club section currently contains the exact supplied `THE CLUB` message and three cards: `SQUASH`, `RECOVER & TRAIN`, and `STAY & CONNECT`, using existing facility/lifestyle imagery. The next section currently repeats `RECOVER & TRAIN` with sauna/cold-plunge imagery and copy; this duplication is the next issue to resolve, but do not remove or redesign it without first deciding where its approved information should live.

## Working method for future edits

Use one narrowly scoped change per request. Before editing, inspect only the relevant file and this brief. If the request changes information architecture or copy placement, ask for confirmation before inventing a replacement. After editing, run the focused desktop/mobile screenshot check and `pnpm check && pnpm build`, then update `todo.md` and save a checkpoint. Do not revisit graphics unless explicitly requested.

## Communication guardrail

When the user reports a visual problem, first identify the exact existing element and explain the proposed correction in one short paragraph. Do not silently reinterpret the request, add copy, add imagery, or change unrelated sections.
